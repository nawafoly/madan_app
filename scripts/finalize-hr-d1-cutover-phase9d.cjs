const fs = require("node:fs");
const path = require("node:path");
const root = process.cwd();
const p = x => path.join(root, x);
const read = x => fs.readFileSync(p(x), "utf8").replace(/^\uFEFF/, "").replace(/\r\n?/g, "\n");
const write = (x,v) => { fs.mkdirSync(path.dirname(p(x)), {recursive:true}); fs.writeFileSync(p(x), String(v).replace(/\r\n?/g,"\n"), "utf8"); };
const once = (s,a,b,label) => { const n=s.split(a).length-1; if(n!==1) throw new Error(`Phase 9D: expected one ${label}, found ${n}`); return s.replace(a,b); };

write("client/src/lib/loginIdentity.ts", `import {\n  buildAdminEmailFromUsername,\n  normalizeAdminUsername,\n} from "@/lib/adminUsername";\nimport { resolveHrCoreLoginIdentity } from "@/lib/hrCoreApi";\n\nexport type LoginIdentityErrorCode =\n  | "username-not-found"\n  | "email-missing";\n\nexport class LoginIdentityError extends Error {\n  code: LoginIdentityErrorCode;\n\n  constructor(code: LoginIdentityErrorCode) {\n    super(code);\n    this.name = "LoginIdentityError";\n    this.code = code;\n  }\n}\n\nexport function normalizeLoginEmail(value: string) {\n  return value.trim().toLowerCase();\n}\n\nexport function isEmailLoginInput(value: string) {\n  return normalizeLoginEmail(value).includes("@");\n}\n\nexport function isLoginIdentityError(error: unknown): error is LoginIdentityError {\n  return error instanceof LoginIdentityError;\n}\n\nfunction getUsernameFallbackDomains() {\n  const configured = String(import.meta.env.VITE_LOGIN_USERNAME_EMAIL_DOMAINS || "")\n    .split(",")\n    .map(domain => domain.trim().toLowerCase())\n    .filter(Boolean);\n\n  return Array.from(new Set([\n    "madanalbena.com",\n    ...configured,\n    "maedin.com",\n    "maedin.sa",\n    "gmail.com",\n  ]));\n}\n\nfunction isHrCoreLookupUnavailable(error: unknown) {\n  const status = Number((error as { status?: unknown })?.status || 0);\n  const message = String((error as { message?: unknown })?.message || "").toLowerCase();\n  return status === 0 || status === 404 || status >= 500 ||\n    message.includes("not configured") ||\n    message.includes("failed to fetch") ||\n    message.includes("network");\n}\n\nexport async function resolveLoginEmailCandidatesForAuth(value: string) {\n  const input = normalizeLoginEmail(value);\n  if (!input) return [];\n  if (input.includes("@")) return [input];\n\n  const username = normalizeAdminUsername(input);\n  if (!username) throw new LoginIdentityError("username-not-found");\n\n  const candidates = new Set<string>();\n\n  try {\n    const result = await resolveHrCoreLoginIdentity(username);\n    const email = normalizeLoginEmail(String(result.email || ""));\n    if (result.emailMissing) throw new LoginIdentityError("email-missing");\n    if (result.found && email) candidates.add(email);\n  } catch (error) {\n    if (isLoginIdentityError(error)) throw error;\n    if (!isHrCoreLookupUnavailable(error)) throw error;\n    console.warn("[HR Login] D1 username lookup unavailable");\n  }\n\n  const generatedEmail = normalizeLoginEmail(buildAdminEmailFromUsername(username));\n  if (generatedEmail) candidates.add(generatedEmail);\n  for (const domain of getUsernameFallbackDomains()) candidates.add(\`\${username}@\${domain}\`);\n\n  if (candidates.size === 0) throw new LoginIdentityError("username-not-found");\n  return Array.from(candidates);\n}\n\nexport async function resolveLoginEmailForAuth(value: string) {\n  const candidates = await resolveLoginEmailCandidatesForAuth(value);\n  if (!candidates[0]) throw new LoginIdentityError("username-not-found");\n  return candidates[0];\n}\n`);

let api=read("client/src/lib/hrCoreApi.ts");
api=once(api, `  return payload;\n}\n\nexport async function getHrCoreMe() {`, `  return payload;\n}\n\nasync function requestPublicHrCore<T>(pathname: string, init: RequestInit = {}): Promise<T> {\n  const hasBody = init.body !== undefined && init.body !== null;\n  const response = await fetch(buildHrCoreUrl(pathname), {\n    ...init,\n    headers: {\n      Accept: "application/json",\n      ...(hasBody ? { "Content-Type": "application/json" } : {}),\n      ...(init.headers || {}),\n    },\n  });\n  const payload = (await response.json().catch(() => null)) as (T & HrCoreApiErrorPayload) | null;\n  if (!response.ok || !payload) {\n    const error = new Error(payload?.message || payload?.detail || \`HR Core public request failed (\${response.status}).\`) as Error & { status?: number; code?: string; payload?: HrCoreApiErrorPayload | null };\n    error.status = response.status;\n    error.code = payload?.message;\n    error.payload = payload;\n    throw error;\n  }\n  return payload;\n}\n\nexport async function resolveHrCoreLoginIdentity(username: string) {\n  return requestPublicHrCore<{ ok: true; found: boolean; email: string | null; emailMissing?: boolean }>(\n    "/api/hr/auth/resolve-login-email",\n    { method: "POST", body: JSON.stringify({ username }) }\n  );\n}\n\nexport async function getHrCoreMe() {`, "HR Core public request anchor");
write("client/src/lib/hrCoreApi.ts", api);

let worker=read("workers/hr-core-worker.js");
worker=once(worker, 'const HR_WORKER_RELEASE = "phase9c-employee-admin-cutover-v1";', 'const HR_WORKER_RELEASE = "phase9d-d1-login-identity-v1";', "worker release");
worker=once(worker, `  if (pathname === "/health" && request.method === "GET") {\n    return healthCheck(env.HR_DB);\n  }\n\n  if (pathname === "/internal/hr/import" && request.method === "POST") {`, `  if (pathname === "/health" && request.method === "GET") {\n    return healthCheck(env.HR_DB);\n  }\n\n  if (pathname === "/api/hr/auth/resolve-login-email") {\n    if (request.method !== "POST") {\n      return json(405, { ok: false, message: "login_identity_method_not_allowed" });\n    }\n    return resolveLoginEmailFromUsername(request, env.HR_DB);\n  }\n\n  if (pathname === "/internal/hr/import" && request.method === "POST") {`, "public login route");
worker=once(worker, `async function routeRequest(request, env) {`, `async function resolveLoginEmailFromUsername(request, db) {\n  const body = await readJsonBody(request, 8192);\n  if (!body.ok) return body.response;\n  const username = normalizeText(body.value?.username).toLowerCase();\n  if (username.length < 2 || username.length > 32 || !/^[a-z0-9._-]+$/.test(username)) {\n    return json(200, { ok: true, found: false, email: null });\n  }\n  try {\n    const account = await db.prepare(\`SELECT email, is_active FROM accounts WHERE LOWER(TRIM(username)) = ? LIMIT 1\`).bind(username).first();\n    if (!account || !Boolean(account.is_active)) return json(200, { ok: true, found: false, email: null });\n    const email = nullableEmail(account.email);\n    if (!email) return json(200, { ok: true, found: true, email: null, emailMissing: true });\n    return json(200, { ok: true, found: true, email });\n  } catch (error) {\n    return serverError("login_identity_lookup_failed", error);\n  }\n}\n\nasync function routeRequest(request, env) {`, "public resolver function");
write("workers/hr-core-worker.js", worker);

for (const f of ["server/hr-firestore-cutover-phase9b.test.ts","server/hr-firestore-cutover-phase9c.test.ts"]) { if (fs.existsSync(p(f))) write(f, read(f).replaceAll("phase9c-employee-admin-cutover-v1", "phase9d-d1-login-identity-v1")); }

write("server/hr-firestore-cutover-phase9d.test.ts", `import fs from "node:fs";\nimport path from "node:path";\nimport { describe, expect, it } from "vitest";\nconst root=process.cwd();\nconst source=(f:string)=>fs.readFileSync(path.join(root,f),"utf8");\ndescribe("HR Firestore cutover phase 9D",()=>{\n  it("removes Firestore username lookup",()=>{ const s=source("client/src/lib/loginIdentity.ts"); expect(s).toContain("resolveHrCoreLoginIdentity"); expect(s).not.toContain("firebase/firestore"); expect(s).not.toContain("firebase/functions"); expect(s).not.toContain("admin_usernames"); });\n  it("adds public D1 resolver",()=>{ const a=source("client/src/lib/hrCoreApi.ts"); const w=source("workers/hr-core-worker.js"); expect(a).toContain("/api/hr/auth/resolve-login-email"); expect(w).toContain("phase9d-d1-login-identity-v1"); expect(w).toContain("LOWER(TRIM(username)) = ?"); });\n});\n`);
write("docs/hr-core-cloudflare-phase9d.md", `# HR Core Cloudflare Phase 9D\n\nRemoves the remaining HR username-login lookup from Firestore and Firebase Functions. Username-to-email resolution now uses the public HR Core Worker route and D1 accounts table. Firebase Auth remains responsible for password verification.\n`);

const login=read("client/src/lib/loginIdentity.ts");
if (login.includes("firebase/firestore") || login.includes("firebase/functions") || login.includes("admin_usernames")) throw new Error("Phase 9D: legacy login lookup remains");
console.log("Phase 9D applied: username login identity now resolves through Cloudflare D1.");
