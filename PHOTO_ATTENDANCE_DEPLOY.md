# نظام الحضور والانصراف بالتصوير

## ما تم تنفيذه

- خيار مستقل داخل كل منطقة عمل في `settings?tab=attendance` باسم **تفعيل الحضور والانصراف بالتصوير**.
- يمكن تفعيله لفرع واحد أو أكثر، بينما تستمر المناطق الأخرى بنظام GPS المعتاد.
- عند تفعيل الخيار للمنطقة المطابقة لموقع الموظف:
  1. يفحص النظام الموقع ودقة GPS.
  2. يفتح الكاميرا الأمامية مباشرة.
  3. يلتقط الموظف صورة.
  4. يعيد النظام فحص الموقع ثم يسجل حضورًا أو انصرافًا ويحفظ الصورة مع السجل.
- الصور تحفظ بصورة خاصة في Cloudflare R2، ولا يوجد لها رابط عام.
- يمكن للإدارة وHR والموظف صاحب السجل عرض الصورة من سجل الحضور بصلاحية مصادقة.
- الحد الأعلى للصورة 2.5 MB، والصيغ المقبولة JPEG وPNG وWebP.

> هذه الميزة توثق صورة لحظة البصمة. لا تنفذ مطابقة وجه أو تعرّفًا بيومتريًا على هوية الموظف.

## التطبيق على البيئة البعيدة

من جذر المشروع افتح PowerShell ثم نفّذ:

```powershell
cd C:\Users\nawaf\Downloads\madan

npx.cmd wrangler d1 migrations apply maedin-attendance --remote --config workers/wrangler.toml
npx.cmd wrangler deploy --config workers/wrangler.toml

npm.cmd install
npm.cmd run check
npm.cmd run build
```

بعد نجاح النشر افتح:

```text
settings?tab=attendance
```

ثم عدّل منطقة العمل المطلوبة، وفعّل خيار **الحضور والانصراف بالتصوير** واحفظها.

## اختبار عملي سريع

1. سجّل الدخول بحساب موظف مرتبط بالمنطقة المفعّل عليها التصوير.
2. اضغط تسجيل الحضور.
3. اسمح للموقع والكاميرا.
4. التقط الصورة وأكد العملية.
5. افتح صفحة سجلات الحضور بحساب HR أو الإدارة، ثم اضغط **عرض صورة البصمة**.
